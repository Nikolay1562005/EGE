from itertools import *

def f(x, y, z, w):
    pass

for i in product([0, 1], repeat=5):
    d = [
        (),
    ]
    if len(set(t)) == len(t):
        for p in permutations('xyzw'):
            if [f(**dict(zip(p,r))) for r in d] == [...]:
                print('')